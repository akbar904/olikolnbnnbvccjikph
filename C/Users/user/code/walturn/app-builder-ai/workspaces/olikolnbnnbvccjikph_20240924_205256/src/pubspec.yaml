name: com.example.counter_app
description: A Flutter application that demonstrates the Cubit pattern for state management with a counter that multiplies by 10.
version: 1.0.0
environment:
  sdk: ">=2.12.0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^7.0.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  bloc_test: ^8.0.0
  mocktail: ^0.9.0
flutter:
  uses-material-design: true